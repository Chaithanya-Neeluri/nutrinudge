# FastAPI and ASGI server
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Data validation and serialization
pydantic==2.5.0

# Python standard library dependencies (usually included, but explicit for clarity)
typing-extensions==4.8.0

# Optional: For better performance and additional features
python-multipart==0.0.6

# For JSON handling (built-in, but listing for completeness)
# json - built-in module

# Python version requirement (add this as a comment for Render.com)
# python_version >= "3.8"
